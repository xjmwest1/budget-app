{
  "name": "budget-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "client": "cd client && yarn dev",
    "server": "cd server && yarn dev",
    "dev": "taskkill /im node.exe && concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "typescript": "^3.1.6"
  }
}
